Write a method prime_factors that takes in a number and returns an array containing all of the prime factors of the given number.


def prime_factors(num)
factors = []
  (2..num).each do |i|
    if num % i == 0
      factors << i
    end
  end
  prime_factors = factors.select{|i| is_prime(i)}
  return prime_factors
end

def is_prime(number)
  divisors = []
  (1..number/2).each do |num|
    if number% num == 0 
      divisors << num
    end
  end
  if divisors.length*2 > 2
    return false
  else
    return true
  end
end
print prime_factors(24) #=> [2, 3]
puts
print prime_factors(60) #=> [2, 3, 5]
puts
