# Feel free to use this variable:
# alphabet = "abcdefghijklmnopqrstuvwxyz"
Write a method caesar_cipher that takes in a string and a number. 
he method should return a new string where every character of the original is shifted num characters in the alphabet.


def caesar_cipher(str, num)
newStr = ""
  alphabet = "abcdefghijklmnopqrstuvwxyz"
  str.each_char do |let|
    indx = alphabet.index(let)
    newIndx= indx+num  
    if newIndx < alphabet.length
    	newStr += alphabet[newIndx]
    else
      newStr += alphabet[newIndx-alphabet.length()]
    end
  end
  return newStr
end

puts caesar_cipher("apple", 1)    #=> "bqqmf"
puts caesar_cipher("bootcamp", 2) #=> "dqqvecor"
puts caesar_cipher("zebra", 4)    #=> "difve"

=========Better Code ============

def caesar_cipher(str, num)
alphabet = "abcdefghijklmnopqrstuvwxyz"
newStr = ""
  str.each_char do |let|
    indx = alphabet.index(let)
    newStr += alphabet[(indx+num)%26]
  end
  return newStr
end

puts caesar_cipher("apple", 1)    #=> "bqqmf"
puts caesar_cipher("bootcamp", 2) #=> "dqqvecor"
puts caesar_cipher("zebra", 4)    #=> "difve"
